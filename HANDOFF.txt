★ FKTI LEARNING · 深い関係学び — HANDOFF (for a new Claude) ★
================================================================
Last updated 2026-07-02. Status: LIVE, in classroom beta. Apps live: Kana, Kanji, Self-Intro, Sumo, Book of Tea.

WHAT THIS IS
Standalone Japanese-learning web apps by FKTI (Fukai Kankei, 深い関係 "deeply connected"; fkti.org).
Brand: "Deeply Connected Learning · 深い関係学び". Single-file, offline-first, no build step.
Greg is a Japanese teacher; the kana trainer is replacing his workbooks and is in use by his class.
Sibling project: the Deeply Connected DINING app at C:\Users\FKTI Work\Downloads\dcd (separate repo —
its own HANDOFF.txt). Don't conflate the two.

LIVE: https://fkti-learning.pages.dev  (custom domain: learning.fkti.org). Apps: /kana.html /kanji.html
  /selfintro.html /sumo.html /book-of-tea.html.
DEPLOY — AUTO from GitHub (as of 2026-07): push to `main` on github.com/fktiapps/fkti-learning →
  Cloudflare Pages builds & deploys automatically. No manual step. (Legacy: was `npm run deploy` /
  wrangler from a local copy — now superseded.)
  ⚠ TWO local clones exist — C:\Users\FKTI Work\Downloads\fkti-learning and C:\pf\fkti-learning — both of
  the SAME remote (github.com/fktiapps/fkti-learning). As of 2026-07-02 the Downloads copy pushed the
  Kanji + Self-Intro + mnemonic-gallery work and is IN SYNC with origin (nothing lost; clean rebase over the
  Book-of-Tea/sumo/caretaker commits). TO AVOID TWO CLAUDES COLLIDING: pick ONE working copy, `git pull
  --rebase` before you start, and don't run two sessions against this repo at once.
  NOTE: kana.html has NO service worker — changes go live on push + a normal refresh.

STANDING RULES: pushes to main auto-deploy; never handle Greg's credentials; bundle only open-licensed
  assets with attribution. (The old "no paid/unattended tokens" rule is superseded for ONE thing: Greg
  authorized the Caretaker agent on 2026-07-02 — see "THE CARETAKER" below.)

DIRECTORY (own git repo)
- index.html ........ landing hub (Deeply-Connected dark theme + logo). Tiles: Kana, Sumo, Kanji, Self
    Introductions, Book of Tea, (soon) Vocabulary.
- kana.html ......... ★ the Kana Trainer (all HTML/CSS/JS in one file, ~440 lines).
- kanji.html ........ ★ the Kanji Trainer — all 2,136 Jōyō (built 2026-06-30). See KANJI below.
- selfintro.html .... ★ Self Introductions 自己紹介 (built 2026-07). See SELF-INTRO below.
- book-of-tea.html + bookoftea-notes.js ... Okakura's 1906 "Book of Tea" full text + tap-to-read notes /
    two-layer close reading (built by the parallel session; not documented in detail here).
- mnem-src/ + mnem-gallery.html ... ORIGINAL kana-mnemonic prototype (NOT wired into kana.html). See MNEMONIC below.
- sumo.html ......... ★ Sumo at Nagoya — tournament companion (single self-contained file, ~900 lines). See SUMO below.
- sumo-src/ ......... build-time generators for sumo.html (Node, re-runnable; no tokens, only free HTTP):
    photos.js .......... -> data/sumo_photos.json (wrestler photo map: Wikimedia CC -> JSA fallback).
    kimarite-photos.js . -> data/kimarite_photos.json + img/sumo/kimarite/*.jpg (PD bout photos; JPEG-validated).
- icons/ ............ logo (icon-192 etc.), copied from the dining app — same brand badge.
- data/kana_assets.json ... {mnem:{hira:{romaji:path},kata:{...}}, credit:{...}} — mnemonic image map.
- data/kanji_joyo.json .... 215 tranches × 10 kanji {k,m,on,ku}; all 2,136 Jōyō, freq-sorted within grade.
    From KANJIDIC2 (CC BY-SA 4.0, EDRDG). kanji.html fetch()es it; built-in Grade-1 (80) is an offline fallback.
- data/sumo_photos.json ... {rikishiId:{url,source,credit,license,page}} — 70 wrestlers (top 2 divisions).
- data/kimarite_photos.json {romaji:{file,license,credit,page}} — 22 public-domain technique photos.
- img/kana/mnem/{hira,kata}/ ... mnemonic images (hira CC0, kata CC BY-SA 3.0). Quality is mediocre.
- img/sumo/kimarite/*.jpg ... 22 PD bout photos (PD-Japan-oldphoto, via Wikimedia Commons).
- kanjivg/<codepoint>.svg ... 2,228 KanjiVG stroke files (CC BY-SA 3.0, Ulrich Apel) — 2,136 Jōyō kanji + 92
    kana — for stroke-order (kana.html + kanji.html). Filenames = 5-hex-digit zero-padded codepoint.

THEME: dining palette — --ink #13182B navy shell, --paper #F7F4EC cream cards, --ver #E2553B accent,
  Mincho display font. Header: logo + "Deeply Connected Learning" + 深い関係学び.

KANA TRAINER — CURRENT STATE (Home menu → sections, ‹ back nav; one screen visible)
 ✅ 📖 Intro — what hiragana/katakana are, how kana works, how to use the app.
 ✅ 🎴 Learn & Write —
    • RECOGNIZE flashcards. ORDER ALGORITHM per row of 5: phase forward (A→O, in order) until all 5
      graded "Know it" → phase reverse (O→A) until all 5 "Know it" → phase random (weighted across all
      introduced). Header shows phase. localStorage 'fkti_kana' per script {rows,phase,pk,cards:{lvl,streak,seen}}.
    • On flip: SOUND AUTO-PLAYS (ja-JP TTS); 🔊/🔇 mute toggle persisted ('fkti_sound'). Mnemonic
      TEXT+IMAGE hidden until 🖼 mnemonic tapped (images are weak, so they're opt-in).
    • QUIZ GATE: unlocking the next row requires a 5-question multiple-choice quiz (kana→romaji),
      perfect score to pass (buildGate/renderGate/addRow).
    • WRITE: finger-tracing over a faint guide char on canvas (Clear / Hide-guide / Next).
 ✅ ✏️ Stroke order — KanjiVG. ▶ Watch animates strokes in order/direction (~2.2s/stroke, slow).
    Draw it → Check grades each stroke for START POINT + DIRECTION + COUNT (catches シ vs ツ).
    Detection: parse SVG <path> per stroke, sample via getPointAtLength on an offscreen measure-svg,
    normalize to 0-1, compare start-dist (<0.26) + direction dot (>0.4). Prev/Next, per script.
 ✅ 🔊 Sounds & Extras — teaching + tap-to-check practicelets: dakuten ゛/handakuten ゜(voiced+bilabial p),
    yōon combos (small ゃゅょ), long vowels (おう/ー), small っ (sokuon).
 ✅ 📊 Progress & decks — GOJŪON CHART color-coded by mastery (green=Know it, yellow=Kinda, orange=Don't
    really, red=No clue, grey=Not started); tap a cell to hear it. RESPONSIVE: portrait = rows by
    consonant; LANDSCAPE/DESKTOP (matchMedia orientation:landscape | min-width:680) = TRADITIONAL vertical
    gojūon (vowels あいうえお down the rightmost column, consonant columns right→left; placed by vowelRow()),
    re-renders on resize/orientationchange. Drives 4 CUSTOM DECKS by confidence band (bandPool):
    💪 boost (lvl3), 🙂 little (≥lvl2), 😤 challenge (≥lvl1), 🔥 hurt (everything); deck grades feed the chart.
 46 hiragana + 46 katakana, each with k/r/m (kana/romaji/text-mnemonic) in DATA{hira:[rows],kata:[rows]}.

SUMO AT NAGOYA — sumo.html (built 2026-06-29 for Greg's class trip to the Nagoya basho, Day 8 / nakabi,
  Sun 19 Jul 2026, IG Arena). Single self-contained file, offline-first, same Deeply-Connected theme.
  Home menu -> sections (‹ back nav), like kana.html. Six sections:
   • Today's Matchups — Day-8 torikumi (Makuuchi/Juryo). • Banzuke — full ranking, tap any wrestler -> bio.
   • Sumo Primer • Ranks & the Banzuke • Ringside Glossary • The 82 Techniques (kimarite).
  LIVE DATA: sumo-api.com (free, CORS open). BASHO='202607', NAKABI_DAY=8. getJSON() caches each response
   to localStorage and falls back to it offline (graceful "not published yet" states until the JSA releases
   the banzuke ~late June and each day's torikumi the evening before). Bios: /rikishi/:id + /:id/stats.
  PHOTOS (wrestlers): pluggable slot in avaSrc() -> CARICATURE[id] (drop-in override) -> PHOTOMAP (Wikimedia
   CC, else JSA) -> stylized placeholder. Map built by sumo-src/photos.js. Re-run after the final banzuke.
  KIMARITE: 82 techniques + 7 non-techniques in KIM[]. Each card = a PHOTO (📷, 22 PD bout photos, duotoned)
   if one exists in KPHOTO, else an ORIGINAL FKTI physics diagram from kdiagram(arch). Searchable/filterable.
  THE DIAGRAMS (kdiagram): articulated crouched rikishi (rik(): 2-segment tapered limbs, mawashi, ōichō
   topknot; o.y = belly centre; feet/hands absolute coords; a()=attacker terracotta face+1, d()=defender slate
   face-1). Each technique maps to an 'arch' (pose, drawn per real grip/footwork) AND a MECH (physics family).
   Overlay shows WHY it works: ● center of gravity, shaded base of support, red force/rotation, ▲ fulcrum,
   ✕ removed base, teal=momentum, + red rings (solid=hold/grip, dashed=push/strike). Legend + physics primer
   in-page. WHY[mech] = one-line explanation on every card. 11 mechanisms; ~27 arch poses.
  SELF-QA THE ART: I can't see SVG by reading it. Render to PNG to check visually:
   npm i @resvg/resvg-js in a SCRATCH dir (NOT the repo), eval the kdiagram() fn out of sumo.html, build a
   contact-sheet SVG, Resvg->PNG, then view. Catches overlaps/pose errors. (resvg is strict XML: escape bare &.)
  STYLE REFERENCE (do NOT copy): sumo.jpg in repo root is the © 2016 SOZOS / JAPAGRA "Top 20" poster Greg
   shared — marked no-reuse. Used only as a style/convention target (athletic figures; red-arrow=movement,
   red-ring=hold). All sumo art is original/procedural FKTI. sumo.jpg is intentionally NOT committed.

KANJI TRAINER — kanji.html (built 2026-06-30). ALL 2,136 Jōyō, same engine/feel as the kana trainer.
  localStorage 'fkti_kanji_v1' {rows,phase,pk,cards:{lvl,streak,seen}}. Home menu → sections:
   • 🎴 Learn & Write — flashcard: kanji → meaning + 音 on'yomi (katakana) / 訓 kun'yomi (hiragana) + ja-JP TTS;
     grade yourself (4 confidence levels). WRITE = finger-trace over a faint guide glyph.
   • ✏️ Stroke order — KanjiVG watch+draw+check (ported from kana; watch speed adapts to stroke count; stroke
     SVGs load lazily w/ retry). On a CORRECT check the card FLIPS to reveal meaning + readings + audio.
   • 📊 Progress & decks — grid grouped by tranche, color-coded by confidence (locked tranches greyed); tap a
     cell to CYCLE through all its readings aloud. 4 confidence-band decks over the unlocked set.
  SRS + UNLOCK: forward→reverse→random phases per TRANCHE of 10. Unlocking the next tranche runs a MIXED quiz =
   a balanced review of ≤20 previously-learned kanji (≤5 per confidence band), mixing 3 modes: kanji→meaning
   (MC), kanji→reading (MC), and DRAW-IT (stroke-order check). Must nail the "strong" (Know-it/Kinda) items to
   pass; weak ones are review-only. MISSING any quiz item DOWNGRADES that kanji one level — this rule was ALSO
   added to the KANA quiz gate. Readings: on in katakana, kun in hiragana, KANJIDIC "-" suffix markers KEPT.

SELF-INTRODUCTIONS — selfintro.html (自己紹介, built 2026-07 for the class trip). localStorage 'fkti_selfintro'.
  4 screens: 📖 Learn (the fixed frame + the 12 option sentences, each kanji·hiragana·romaji·English + TTS),
  ✍️ Build, 🎤 Practice, 🗣️ Say it in Japanese.
   • BUILD: name + choose a greeting (はじめまして…どうぞよろしく | よろしくおねがいします) + PICK UP TO 3 of 12 prompts
     (age, hobby, food, anime, music, song, fav-thing-about-Japan, best-thing-seen, a-surprise, unique-fact,
     homestay, school-vs-KGN). Student types answers in ENGLISH; ✦ Translate → Japanese (cached in the save).
     Script order: Hello → My name is ___ → greeting → 3 picks → ありがとうございます. Age is native-computed
     (kanji + reading); the picks are machine-translated.
   • SAY IT: free English → Japanese with FURIGANA (ruby); tap a word = speak + English gloss; auto vocab list.
  TRANSLATION/FURIGANA = FREE PROTOTYPE (explicitly flagged to Greg): translate via MyMemory (keyless, CORS-ok,
   DAILY CHAR LIMIT — a class could exhaust it); segment+furigana+readings via kuromoji.js from jsDelivr (~15 MB
   dict, one-time, browser-cached). Loader is preloaded + cached + 30s timeout; readings BACK-FILL onto saved
   lines once the dict is ready (fixed a bug where picks showed kanji in the hiragana/romaji views); degrades to
   translation+audio if the dict can't load. THE RELIABLE UPGRADE (Greg's call — needs a key + small cost):
   a Cloudflare Pages FUNCTION calling the Claude API to translate+segment+furigana+gloss in ONE call. NOT
   built (it would end the pure-static model; the free path stays until Greg decides).

MNEMONIC ART PROTOTYPE — mnem-src/ + mnem-gallery.html (2026-06-27..30; NOT wired into kana.html; standalone).
  Answers roadmap #3 (original, licensing-free FKTI kana mnemonics). TWO styles per character: STATIC (a hook
  drawn INTO the shape) and MOTION ("say it while you draw" — the char draws itself stroke-by-stroke on the real
  KanjiVG vectors, ONE chant beat per stroke, draw→hold→disappear→redraw loop). Greg art-directed ~あ–た with
  specifics (な = "na na na na / hey hey hey / 👋 goodbye"; か・た blink a red romaji "Ka/Ta" overlay; と = thorn
  in the lion's toe). mnem-src/generate.js embeds the per-kana content + emits the gallery from strokes.json;
  `node mnem-src/generate.js` rebuilds mnem-gallery.html. PRINCIPLE enforced by the build: one stroke = one
  continuous motion = exactly one chant beat (build errors if a chant's beats ≠ its stroke count). Next step:
  finish the set (た-row on) + decide whether/how to fold it into the kana app.

DEV: edit the app's single HTML file directly; validate JS (new Function on each <script>) before committing;
  commit (author FKTI/gjswork@yahoo.com); PUSH TO main AUTO-DEPLOYS (Cloudflare Pages git integration — no
  manual step). Known gotcha (fixed): the .rate button{color:#fff}
  rule needs a .rate .ghost{color:var(--muted)} override or ghost buttons go white-on-white.

ASSETS/LICENSING: hira mnemonic images CC0; kata CC BY-SA 3.0; KanjiVG CC BY-SA 3.0 (Ulrich Apel).
  Mnemonic IMAGES are mediocre ("terrible" per Greg) → hidden behind 🖼, text mnemonics lead. A search
  for better CC/MIT sets (MaruKana etc.) found nothing usable; the good ones are proprietary. IDEA on
  the table (Greg curious): generate an ORIGINAL FKTI-owned consistent SVG mnemonic set (hook drawn into
  each character's shape) — sidesteps licensing. Not started (image-gen = token cost).

ROADMAP / TODO
1. 📝 WORDS / VOCABULARY module — Greg will SUPPLY word lists (likely from class material). Reuse the
   flashcard + SRS + tracing + chart engine for words. Cheap once lists are provided. Tile shows "soon".
2. ★ GOOGLE CLASSROOM integration — Greg's long-term direction (he leans toward full integration).
   • Route A (cheap, no Google setup): Greg exports/shares materials → convert structured vocab/phrases
     to decks; surface prose as readable material. Doubles as the Words module.
   • Route B (heavy, what he eventually wants): in-app Google sign-in (Google Identity Services OAuth) +
     Classroom REST API (courses/coursework/announcements readonly) + Drive API for attached Docs/Slides.
     REQUIRES Greg's admin steps: Google Cloud project, OAuth client, consent screen, ENABLE APIs, and
     GOOGLE APP-VERIFICATION for read scopes. Materials mostly embed/link, not auto-flashcard. Student
     sign-in pulls personal data (privacy/FERPA flag). Scope with Greg before building.
3. Original FKTI mnemonic art set — PROTOTYPE built (mnem-src/ + mnem-gallery.html, see MNEMONIC above);
   finish あ→ん in both styles, then decide whether to wire it into kana.html.
4. PWA: add manifest + service worker for installable/offline (good for classroom devices).
5. Polish from class feedback (watch nav, order progression, stroke detection).
6. Self-Intro RELIABLE TRANSLATION — if the free MyMemory/kuromoji path's daily limit or ~15 MB dict bite in
   class, swap to a Cloudflare Pages Function → Claude API (translate+furigana+gloss in one call). Greg's call.

NEXT (2026-07-02 handoff): one session (the Downloads clone) built Kanji + Self-Intro + the mnemonic prototype
  and pushed them; a parallel session/agent added Book of Tea + sumo dossiers + the Caretaker. All merged &
  live. CONSOLIDATE to ONE working copy + one active Claude (see the ⚠ under DEPLOY) so we stop colliding on
  pushes. Cheapest remaining wins: the Words/Vocabulary module (reuses the kana+kanji SRS/quiz/chart engine)
  and finishing the mnemonic set. Sibling DINING app is a separate repo (Downloads/dcd) with its own HANDOFF.

================================================================
★ THE CARETAKER — low-cost scheduled agent (BUILT 2026-07-02) ★
================================================================
An explicitly Greg-AUTHORIZED exception to the old "no paid/unattended tokens" rule: Greg approved a small
autonomous maintenance bot (budget ~$10/mo; he's fine ~$3-5). It keeps the FKTI apps fresh & accurate by
doing ONE tiny "beat" per scheduled run.

LIVES IN: caretaker/ + .github/workflows/caretaker.yml (in this repo).
 - caretaker/ledger.json .... rotating checklist of "watches" (id, active, cadenceDays, lastChecked,
     instructions). Each beat picks the oldest DUE active watch. Active: `sumo-gossip`; `vegan-dining`
     (DISCOVERY→REVIEW — finds veg spots for the trip cities via the LIVE fkti-dining-app data at
     https://fkti-dining-app.pages.dev/data/<city>.json so it doesn't duplicate, stages them in
     caretaker/dining-candidates.md for Greg to vet + add himself; NEVER auto-published, per celiac-
     safety-#1 + the dining app's by-hand deploy). Dormant (need the TRAVEL repo): `itinerary-links`,
     `near-today`. The dining repo is github.com/fktiapps/fkti-dining-app (local: Downloads/dcd).
 - caretaker/BEAT.md ........ the brief the agent runs each beat: pick oldest due watch → verify via web →
     apply FACTUAL updates + cite sources; SENSITIVE items (allegations/rumors about named people) go to
     review-queue, NEVER auto-published; append 1 line to changelog; bump lastChecked; STOP. One watch/run.
 - caretaker/changelog.md ... the visible "what changed" log.
 - caretaker/review-queue.md  human-review holding pen for sensitive finds (nothing here is live).
 - .github/workflows/caretaker.yml  daily cron (+ manual "run a beat" button). Installs Claude Code,
     runs `claude -p "$(cat caretaker/BEAT.md)"` on Haiku (cheap), commits any change with the default
     GITHUB_TOKEN (permissions: contents: write).

HOW IT RUNS / COST: it is DORMANT until an ANTHROPIC_API_KEY repo secret is set (job has
  `if: secrets.ANTHROPIC_API_KEY != ''` so it skips cleanly and costs $0 until then). Runs in the CLOUD
  (GitHub Actions) so it does NOT depend on Greg's laptop being on. Est. ~$2-5/mo on Haiku at daily cadence;
  two dials to tune cost: cadence (cron) and model tier. IMPORTANT: also set a hard SPEND CAP in the
  Anthropic console as a runaway guard.

TO TURN ON (August, when Greg's Max sub ends — the API is separate billing, the sub can't fuel this):
  1. Create an Anthropic API key + set a monthly spend cap in console.anthropic.com.
  2. Add it as repo secret ANTHROPIC_API_KEY (Settings → Secrets and variables → Actions).
  3. Actions tab → FKTI Caretaker → "Run workflow" to test one beat; confirm the claude CLI flags against
     the installed version (tweak if the headless flags changed) and that the commit/push works.
BEFORE AUGUST: Greg's Max subscription can run beats for free INTERACTIVELY (ask Claude Code to
  "run a caretaker beat" following caretaker/BEAT.md) — same ledger/rules, no API cost.
TO EXTEND to vegan-dining / itinerary: the Action must also check out the dining/travel repos (add a
  checkout step with a PAT that has access), then flip those watches `active: true` in the ledger.
DEPLOY NOTE: confirm the learning site auto-deploys from git pushes (Cloudflare Pages git integration) —
  if Greg still deploys manually via `npm run deploy`, the caretaker's commits won't go live until a deploy.
