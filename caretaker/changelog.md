# FKTI Caretaker — changelog

Everything the caretaker touches, newest at the bottom. This is the "what's changed" record.

- 2026-07-02 · install · Caretaker framework installed (ledger, beat brief, review queue, daily workflow). The sumo companion is feature-complete, so the first active watch is `sumo-gossip`. Cloud beats begin only once an `ANTHROPIC_API_KEY` secret is set on the repo (until then it stays dormant and costs nothing).
- 2026-07-02 · vegan-dining · Activated as DISCOVERY→REVIEW: finds veg-friendly spots for trip cities via the live fkti-dining-app data and stages them in caretaker/dining-candidates.md for Greg to vet + add (never auto-published; celiac safety).
